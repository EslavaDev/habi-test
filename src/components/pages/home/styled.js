import { Card } from 'react-bootstrap';
import styled from 'styled-components';

export const WrapperHomeStyled = styled(Card)`
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
  .home__col {
    margin: 10px 0px;
  }
`;
